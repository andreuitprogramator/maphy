services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: maphy
    ports:
      - "5432:5432"
    volumes:
      - maphy_pgdata:/var/lib/postgresql/data

volumes:
  maphy_pgdata:

